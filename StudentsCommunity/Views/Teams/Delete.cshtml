@model StudentsCommunity.Models.Team

@{
    ViewBag.Title = "Delete";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h3 class="alert-danger">Are you sure you want to delete this Team? <i class="fa fa-warning"></i></h3>
<div>
    <h4 class="note">Team Information</h4>

    <table class="table table-striped table-bordered table-inverse" style="width:100%">
        <tbody>

            <tr>
                <td>
                    @Html.DisplayNameFor(model => model.Name)
                </td>
                <td>
                    @Html.DisplayFor(model => model.Name)
                </td>
            </tr>
        </tbody>
    </table>

    @using (Html.BeginForm())
    {
        @Html.AntiForgeryToken()

        <div class="form-actions no-color">
            <input type="submit" value="Delete" class="btn btn-danger" /> 
            @*<a href="@Url.Action("MyTeams", "Teams")" class='btn btn-info btn-xs'>
                <i class="fa fa-arrow-left"></i>
                back
            </a>*@
        </div>
    }
</div>
