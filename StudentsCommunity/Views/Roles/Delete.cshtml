@model Microsoft.AspNet.Identity.EntityFramework.IdentityRole

<h3 class="alert-danger">Are you sure you want to delete this Role? <i class="fa fa-warning"></i></h3>
<div style="margin:5px">
    <h4 class="note">Role Information</h4>

    <table class="table table-striped table-bordered table-inverse" style="width:100%">
        <tbody>

            <tr>
                <td>
                    @Html.DisplayNameFor(model => model.Name)
                </td>
                <td>
                    @Html.DisplayFor(model => model.Name)
                </td>
            </tr>
            <tr>
        </tbody>
    </table>

    @using (Html.BeginForm())
    {
        @Html.AntiForgeryToken()

        <div class="form-actions no-color">
            <input type="submit" value="Delete" class="btn btn-danger" /> 
        </div>
    }
</div>

<div class="form-actions no-color">
    <a href="@Url.Action("Index", "Roles")" class='btn btn-info btn-xs'>
        <i class="fa fa-arrow-left"></i>
        back
    </a>
    <a href="@Url.Action("Edit", "Roles", new { id = Model.Id } )" class='btn btn-success btn-xs'>
        <i class="fa fa-edit"> Edit</i>
    </a>
</div>